apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
data:
  test: test123
  name: {{ .Files.Get "files/new.txt" }}
  index: "{{ index .Files "files/new.txt" | toString }}"
  bytes: "{{ .Files.GetBytes "files/new.txt" }}"

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-1
data:
  {{ (.Files.Get "files/new.txt").AsConfig }}

---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-1
data:
  {{ (.Files.Get "files/new.txt").AsSecrets }}


